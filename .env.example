# ── DATABASE ──────────────────────────────────────────────────────────────────
# Stringa di connessione PostgreSQL. Per sviluppo locale puoi usare Neon free tier.
# https://neon.tech — crea un progetto e copia la connection string con ?sslmode=require
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"

# ── AUTH ──────────────────────────────────────────────────────────────────────
# Genera con: openssl rand -base64 32
AUTH_SECRET="your-secret-here"

# URL pubblico dell'applicazione. Obbligatorio in produzione.
# Sviluppo locale: http://localhost:3000
# Accesso da rete locale: http://<LAN-IP>:3000
# Produzione Vercel: https://<tuo-progetto>.vercel.app
AUTH_URL="http://localhost:3000"

# ── GOOGLE BOOKS API ──────────────────────────────────────────────────────────
# Opzionale ma fortemente consigliato: senza API key il quota è 100 req/giorno.
# Con API key: 1000 req/giorno (free tier).
# https://console.cloud.google.com → "Books API" → Credenziali → Chiave API
GOOGLE_BOOKS_API_KEY=""

# ── GROQ AI ───────────────────────────────────────────────────────────────────
# Obbligatorio per la Sanctuary Chat (AI integrata). Piano gratuito generoso.
# https://console.groq.com → crea una API key (nessuna carta richiesta)
GROQ_API_KEY=""
